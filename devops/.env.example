# ================================================
# PostgreSQL Configuration
# ================================================
# Use this file when running docker-compose.postgres.yml
# Copy this file to .env and update the values

# Database Name
POSTGRES_DB=ramadan_db

# PostgreSQL Admin Password (for superuser 'postgres')
# SECURITY: Use a strong password with at least 20 characters
# This user should only be used for administrative tasks
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_ADMIN_PASSWORD_MIN_20_CHARS

# Application User Password (for 'ramadan_app' user)
# SECURITY: Use a different strong password with at least 20 characters
# This user is used by the backend application to connect to the database
APP_USER_PASSWORD=CHANGE_THIS_STRONG_APP_PASSWORD_MIN_20_CHARS

# Monitoring User Password (for 'monitor' user)
# This user has read-only access for health checks and monitoring
MONITOR_USER_PASSWORD=CHANGE_THIS_MONITOR_PASSWORD_MIN_16_CHARS

# ================================================
# IMPORTANT NOTES:
# ================================================
# 1. The postgres user is the superuser - use for admin tasks only
# 2. The ramadan_app user is for backend connections - restricted privileges
# 3. The monitor user is for health checks - read-only access
# 4. Never use the same password for different users
# 5. Store this .env file securely and never commit it to version control
# ================================================

