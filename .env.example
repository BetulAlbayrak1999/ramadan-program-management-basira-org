# ================================================
# Ramadan Program Management - Application Configuration
# ================================================
# Copy this file to .env and update with your actual values
# NEVER commit the .env file to version control

# ================================================
# DATABASE CONFIGURATION
# ================================================
# Database connection URL for the application
# Format: postgresql://username:password@host:port/database
# 
# For Docker: use 'postgres' as hostname (service name in docker network)
# For Local: use 'localhost' as hostname
#
# NOTE: Use the APPLICATION USER (ramadan_app), NOT the admin user
DATABASE_URL=postgresql://ramadan_app:CHANGE_THIS_APP_PASSWORD@postgres:5432/ramadan_db

# Database name (must match POSTGRES_DB in devops/.env)
POSTGRES_DB=ramadan_db

# Application user password (must match APP_USER_PASSWORD in devops/.env)
# This is the password for the 'ramadan_app' database user
APP_USER_PASSWORD=CHANGE_THIS_STRONG_APP_PASSWORD_MIN_20_CHARS

# ================================================
# JWT CONFIGURATION (Authentication)
# ================================================
# Secret key for JWT token generation
# SECURITY: Generate with: openssl rand -hex 32
JWT_SECRET_KEY=GENERATE_WITH_openssl_rand_hex_32

# Token expiration time in seconds (default: 30 days)
JWT_ACCESS_TOKEN_EXPIRES=2592000

# ================================================
# EMAIL CONFIGURATION (SMTP)
# ================================================
# Email server settings for sending notifications
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true

# Email credentials
# For Gmail: use an App Password (not your regular password)
# https://support.google.com/accounts/answer/185833
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password

# ================================================
# SUPER ADMIN ACCOUNT
# ================================================
# This account is automatically created on first startup
SUPER_ADMIN_EMAIL=admin@yourdomain.com
SUPER_ADMIN_PASSWORD=CHANGE_THIS_ADMIN_PASSWORD_MIN_16_CHARS

# ================================================
# APPLICATION SETTINGS
# ================================================
# Enable or disable email notifications
ENABLE_EMAIL_NOTIFICATIONS=true

# ================================================
# SECURITY CHECKLIST BEFORE DEPLOYMENT:
# ================================================
# [ ] Changed all passwords to strong, unique values
# [ ] Generated a secure JWT_SECRET_KEY using openssl
# [ ] Updated SUPER_ADMIN_EMAIL to your real email
# [ ] Configured email settings for your SMTP provider
# [ ] APP_USER_PASSWORD matches the one in devops/.env
# [ ] Never committed this .env file to git (check .gitignore)
# [ ] Restricted file permissions: chmod 600 .env
# ================================================

